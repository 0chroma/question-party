questionparty.com didn't work, so we decided to redo it.

This is a Python WSGI app. Requires the following:

python
twisted (python module)
twisted-web

to run, do the following command:
    twistd -ny qp/questionparty.tac

